# Lean runtime install for users (clone and run). For pytest/ruff use requirements-dev.txt
# or: pip install -e ".[dev]"
requests>=2.31.0
python-dotenv>=1.0.0
browser-cookie3>=0.20.0
howlongtobeatpy>=1.0.0
psnawp>=3.0.0
websocket-client>=1.6.0
keyring>=25.0.0
cryptography>=50.0.0
PyJWT>=2.8.0
# Optional: system tray launcher (tray_app.py). Server runs fine without these.
pystray>=0.19
Pillow>=12.3.0
